# VEX Events API
VEX_TOKENS=token_1,token_2
# Optional override for season; leave unset to use latest season during incremental runs.
# VEX_SEASON_ID=
VEX_EVENT_START=2025-12-17T00:00:00

# Preferred DB config (all databases)
DATABASE_URL=

# SQL Server fallback config if DATABASE_URL is empty
DB_USER=
DB_PASS=
DB_HOST=
DB_NAME=
